# Generated by Fabric
# vars: $$name, $$package_root, $$settings_fp

#!/bin/bash
# This hook is run after this virtualenv is activated.

# Set Django environment
[[ -f ${settings_fp} ]] && export DJANGO_SETTINGS_MODULE=${name}.settings

# Set node.js bin
npm_bin=${package_root}/node_modules/.bin
[[ -d $$npm_bin ]] && export PATH=$$npm_bin:$$PATH
