# Generated by Fabric
# vars: $$settings_fp, $$project_name

#!/bin/bash
# This hook is run after this virtualenv is activated.

if [[ -f $settings_fp ]]; then
  export DJANGO_SETTINGS_MODULE=${project_name}.settings;
fi;
